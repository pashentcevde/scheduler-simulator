# Опорный прогон: ровный смешанный поток, ~85% ёмкости кластера.
# С него начинают: относительно него меряют всё остальное.
name: s01_steady_mixed
description: Ровный пуассоновский поток от трёх команд
seed: 42
time_scale: 240              # 1 сим-минута = 1 реальная секунда
duration_sim_minutes: 720   # 12 сим-часов -> ~12 минут реального времени
max_jobs: 400
tenants_preset: standard
